# Quiz de Revisao: DIVs e Tabelas

Revise os conceitos de organizacao com `div` e tabelas simples.

---

<quiz>
Qual tag e mais usada para criar blocos genericos de organizacao no HTML?

* [ ] `<table>`
* [ ] `<section>`
* [x] `<div>`
* [ ] `<footer>`
</quiz>

<quiz>
Qual atributo deve ser unico por elemento na pagina?

* [x] `id`
* [ ] `class`
* [ ] `style`
* [ ] `src`
</quiz>

<quiz>
Em tabelas HTML, qual tag define uma celula de cabecalho?

* [ ] `<td>`
* [ ] `<tr>`
* [x] `<th>`
* [ ] `<tc>`
</quiz>

<quiz>
Qual combinacao descreve melhor uma estrutura de tabela simples?

* [x] `<table>`, `<tr>`, `<th>` e `<td>`
* [ ] `<grid>`, `<line>`, `<head>` e `<cell>`
* [ ] `<table>`, `<row>`, `<col>` e `<data>`
* [ ] `<list>`, `<li>`, `<th>` e `<td>`
</quiz>

<quiz>
Qual pratica melhora a organizacao da pagina usando `div`?

* [ ] Misturar todo o conteudo sem separacao.
* [ ] Repetir o mesmo `id` em varios blocos.
* [x] Separar topo, conteudo e rodape em blocos diferentes.
* [ ] Usar apenas tabela para todo o layout.
</quiz>

<!-- mkdocs-quiz results -->
